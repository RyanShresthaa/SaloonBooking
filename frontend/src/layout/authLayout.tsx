import AuthRouteMotion from '@/components/motion/AuthRouteMotion';

/** Centers login / register / verify-email within the main shell. */
export default function AuthStack() {
  return <AuthRouteMotion />;
}
